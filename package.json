{
  "name": "Ooee",
  "description": "Design System Toolkit",
  "private": true,
  "license": "MIT",
  "scripts": {
    "docs-cleanup": "rm -fr _site",
    "docs-build": "yarn docs-cleanup && eleventy",
    "docs-dev": "yarn docs-build --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-navigation": "^0.3.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.6"
  },
  "workspaces": [
    "packages/*"
  ]
}
